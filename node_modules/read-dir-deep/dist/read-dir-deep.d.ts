import { GlobbyOptions } from 'globby';
import { defaultIgnorePatterns } from './parse-args';
export interface Options extends GlobbyOptions {
    patterns?: string[];
    cwd?: string;
}
declare function readDirDeep(rootDir: string, options?: Options): Promise<string[]>;
declare function readDirDeepSync(rootDir: string, options?: Options): string[];
export { readDirDeep, readDirDeepSync, defaultIgnorePatterns };
//# sourceMappingURL=read-dir-deep.d.ts.map